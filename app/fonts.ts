import { Averia_Libre, Loved_by_the_King } from 'next/font/google';

export const charm = Loved_by_the_King({
  subsets: ['latin'],
  weight: ['400'],
  display: 'swap',
});

export const inter = Averia_Libre({
  subsets: ['latin'],
  display: 'swap',
  weight: ['300'],
});
